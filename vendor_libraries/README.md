# Vendor Libraries

This directory contains generated SQLite databases from real vendor archives.
They are strictly local and gitignored.

Run `python scripts/setup_vendor_models.py` to generate the production DB here.
