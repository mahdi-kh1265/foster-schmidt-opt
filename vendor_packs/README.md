# Vendor Packs

This directory contains real vendor component archives (e.g. Coilcraft, Murata).
These archives are intentionally local and gitignored due to redistribution rights.

To populate the local library:
1. Place downloaded ZIPs in `~/Downloads` or here.
2. Run `python scripts/setup_vendor_models.py`.
